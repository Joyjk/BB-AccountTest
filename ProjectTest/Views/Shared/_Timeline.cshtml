@model AccountOpeningModel

<div class="timeline" >
    <div class="step @(Model.CurrentStep >= 1 ? "active" : "")">
        <div class="step-number">1</div>
        <div class="step-title">OTP Verification</div>
    </div>

    <div class="step @(Model.CurrentStep >= 2 ? "active" : "")">
        <div class="step-number">2</div>
        <div class="step-title">NID Verification</div>
    </div>

    <div class="step @(Model.CurrentStep >= 3 ? "active" : "")">
        <div class="step-number">3</div>
        <div class="step-title">Face Verification</div>
    </div>

    <div class="step @(Model.CurrentStep >= 4 ? "active" : "")">
        <div class="step-number">4</div>
        <div class="step-title">Contact info</div>
    </div>

    <div class="step @(Model.CurrentStep >= 5 ? "active" : "")">
        <div class="step-number">5</div>
        <div class="step-title">Nominee Info</div>
    </div>

    <div class="step @(Model.CurrentStep >= 6 ? "active" : "")">
        <div class="step-number">6</div>
        <div class="step-title">Document Upload</div>
    </div>

    <div class="step @(Model.CurrentStep >= 7 ? "active" : "")">
        <div class="step-number">7</div>
        <div class="step-title">Preview & Submit</div>
    </div>
</div>